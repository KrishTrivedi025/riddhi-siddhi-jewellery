<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme (unused but required) -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>

        <!-- ✅ REMOVED: android:windowTranslucentNavigation -->
        <!-- That line was forcing edge-to-edge and making safe-area-inset = 0 -->

        <!-- ✅ Keep system bar drawing but NOT translucent -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:navigationBarColor">#FF0F0F0F</item>
        <item name="android:statusBarColor">#FF0F0F0F</item>
    </style>

    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
    </style>

</resources>